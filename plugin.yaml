name: harbor
version: 1.0.0
description: >
  Advisory collaboration-pattern router for multi-agent work (solo / pair / swarm).
  Helps agents decide when coordination cost is worth paying before launching
  delegate_task or multi-agent swarms. Cache-safe — no automatic hooks.
author: Aiona Edge / SMF Works
provides_tools:
  - harbor_recommend
  - harbor_status
  - harbor_self_test
provides_commands:
  - harbor
provides_cli_commands:
  - harbor
provides_skills:
  - collaboration-pattern-router
